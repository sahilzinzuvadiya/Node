<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Movie Form</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #121212;
      /* Dark theme */
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .form-container {
      background: #1e1e1e;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(255, 0, 0, 0.4);
      width: 450px;
    }

    .form-container h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #ffcc00;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: bold;
      color: #ddd;
    }

    .form-field {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: none;
      border-radius: 6px;
      background: #2a2a2a;
      color: #fff;
      font-size: 15px;
      display: block;
    }

    .form-field:focus {
      outline: none;
      border: 2px solid #ffcc00;
      background: #333;
    }

    textarea.form-field {
      resize: none;
    }

    .btn-group {
      display: flex;
      justify-content: space-between;
      gap: 15px;
    }

    .btn {
      flex: 1;
      padding: 12px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.3s;
    }

    .btn-save {
      background: linear-gradient(45deg, #28a745, #3cb371);
      color: white;
    }

    .btn:hover {
      transform: scale(1.05);
      box-shadow: 0 0 12px rgba(255, 255, 255, 0.3);
    }
</style>
</head>

<body>

  <div class="form-container">
    <h2>✏️ Edit Movie</h2>
    <form action="/updateData" method="post" enctype="multipart/form-data">
      <input style="margin-bottom: 20px;" type="file" name="image">
      <img src="<%=singleData.image%>" alt="">

      <input type="text" name="id" value="<%=singleData.id%>" hidden>

      <label for="title">Movie Title</label>
      <input type="text" id="title" name="title" class="form-field" value="<%=singleData.title%>">

      <label for="genre">Genre</label>
      <select id="genre" name="genre" class="form-field" value="<%=singleData.genre%>">
        <option>Action</option>
        <option selected>Science Fiction</option>
        <option>Drama</option>
        <option>Comedy</option>
        <option>Horror</option>
        <option>Romance</option>
      </select>

      <label for="rating">Rating</label>
      <input type="number" id="rating" name="rating" class="form-field" value="<%=singleData.rating%>" min="1" max="10">

      <label for="review">Review</label>
      <textarea id="review" name="review" class="form-field" rows="4"><%=singleData.review%></textarea>

      <div class="btn-group">
        <button type="submit" class="btn btn-save">💾 Save</button>
        
      </div>
    </form>
  </div>

</body>

</html>