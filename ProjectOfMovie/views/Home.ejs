<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie Navbar</title>
  <style>
    /* Reset some default styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #121212;
      color: white;
    }

    /* Navbar styling */
    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 40px;
      background: linear-gradient(90deg, #1a1a1a, #0d0d0d);
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 4px 15px rgba(0,0,0,0.5);
    }

    /* Logo */
    .logo {
      display: flex;
      align-items: center;
      font-size: 22px;
      font-weight: bold;
      color: #ffcc00;
      letter-spacing: 1px;
    }
    .logo img {
      height: 40px;
      margin-right: 10px;
    }

    /* Navbar links */
    .nav-links {
      display: flex;
      gap: 45px;
    }
    .nav-links a {
      color: #ddd;
      text-decoration: none;
      font-size: 18px;
      transition: 0.3s;
      position: relative;
    }
    .nav-links a::after {
      content: '';
      display: block;
      width: 0;
      height: 2px;
      background: #ffcc00;
      transition: width 0.3s;
      position: absolute;
      bottom: -5px;
      left: 0;
    }
    .nav-links a:hover {
      color: #ffcc00;
    }
    .nav-links a:hover::after {
      width: 100%;
    }

    /* Movie button */
    .movie-btn {
      background: linear-gradient(45deg, #ff4500, #ff6347);
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      font-weight: bold;
      font-size: 16px;
      transition: transform 0.2s, box-shadow 0.3s;
    }
    .movie-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 0 15px #ff6347;
    }


.movie-container {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 20px;
  padding: 20px;
}

.movie-card {
  background: #1e1e1e;
  border-radius: 12px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.6);
  overflow: hidden;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.movie-card:hover {
  transform: translateY(-5px) scale(1.03);
  box-shadow: 0 0 20px rgba(255, 99, 71, 0.8),   /* tomato glow */
              0 0 40px rgba(255, 99, 71, 0.6);  /* tomato spread */
}

.movie-img {
  width: 100%;
  height: 180px;
  object-fit: cover;
  border-bottom: 1px solid #333;
}

.movie-info {
  padding: 15px;
}

.movie-info h2 {
  margin: 0 0 10px;
  font-size: 20px;
  color: #f5f5f5;
}

.movie-info p {
  margin: 5px 0;
  font-size: 14px;
  color: #bbb;
}

.movie-actions {
  display: flex;
  justify-content: space-around;
  padding: 10px 0 15px;
}

.btn {
  text-decoration: none;
  padding: 8px 14px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: bold;
  transition: background 0.3s ease;
}

.edit-btn {
  background: #007bff;
  color: white;
}

.edit-btn:hover {
  background: #0056b3;
}

.delete-btn {
  background: #dc3545;
  color: white;
}

.delete-btn:hover {
  background: #a71d2a;
}



  </style>
</head>
<body>

  <!-- Navigation Bar -->
  <nav>
    <div class="logo">
      <img src="https://cdn-icons-png.flaticon.com/512/2798/2798007.png" alt="Movie Logo">
      CineWorld
    </div>

    <div class="nav-links">
      <a href="#">Home</a>
      <a href="#">Movies</a>
      <a href="#">Series</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
    </div>

    <a href="/MovieForm"><button class="movie-btn">🎬 Add Movie</button></a>
  </nav>
  <div class="movie-container">
  <% data.map(movie => { %>
    <div class="movie-card">
      <img src="<%= movie.image %>" alt="<%= movie.title %>" class="movie-img">

      <div class="movie-info">
        <h2><%= movie.title %></h2>
        <p><strong>Genre:</strong> <%= movie.genre %></p>
        <p><strong>Rating:</strong> ⭐ <%= movie.rating %>/10</p>
        <p><strong>Review:</strong> <%= movie.review %></p>
      </div>

      <div class="movie-actions">
        <a href="/editData?id=<%= movie._id %>" class="btn edit-btn">✏️ Edit</a>
        <a href="/deleteData?id=<%= movie._id %>" class="btn delete-btn" onclick="return confirm('Are you sure?')">🗑 Delete</a>
      </div>
    </div>
  <% }) %>
</div>


</body>
</html>
